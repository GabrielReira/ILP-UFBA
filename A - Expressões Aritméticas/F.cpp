#include <iostream>
using namespace std;

int main() {
    int long l, z, n, u, s, total;

    cin >> l >> z >> n >> u >> s;
    total = l + z + n + u + s;

    cout << total << endl;

    return 0;
}